{% extends "OpiferBlogBundle::base.html.twig" %}

{%  block body %}
    {#{{ dump(comment.blog.id) }}#}

    <form action="{{ path('opifer_blog_comment_create', { 'blog_id' : comment.blog.id }) }}" method="post" {{ form_enctype(form) }} class="opifer">
        {{ form_widget(form) }}
        <p>
            <input type="submit" value="Submit">
        </p>
    </form>

{% endblock %}